theory+impl both
rest stateless
springboot (beans working and ioc)
jackson 
sql
native and hibernate method
annotations 
validations 
3rd party api's 
docker 
jenkins
kafka
you can read blogs of other comp

do this prepare it also :- 	case
		when t.transaction_status = 2 then 'PROCESSING'
		when t.transaction_status = 3 then 'SUCCESS'
		else 'FAILED'
	end as "Transaction Status",